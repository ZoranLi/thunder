package com.alibaba.fastjson.serializer;

/* compiled from: AfterFilter */
public abstract class c implements aw {
    static final ThreadLocal<af> a = new ThreadLocal();
    static final ThreadLocal<Character> b = new ThreadLocal();
    private static final Character c = Character.valueOf(',');
}
